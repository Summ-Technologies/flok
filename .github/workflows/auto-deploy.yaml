name: Auto deploy (production)
on:
  push:
    branches:
      - staging
jobs:
  changes:
    runs-on: ubuntu-latest
    # Set job outputs to values from filter step
    outputs:
      flok: ${{ steps.filter.outputs.flok }}
      flok-admin: ${{ steps.filter.outputs.flok-admin }}
    steps:
      - uses: actions/checkout@v2
      - uses: dorny/paths-filter@v2
        id: filter
        with:
          filters: |
            flok:
              - 'modules/flok/**'
            flok-admin:
              - 'modules/flok-admin/**'
  auto-deploy-flok-staging:
    needs: changes
    if: ${{ needs.changes.outputs.flok== 'true' && github.ref == 'refs/heads/staging' }}
    uses: ./.github/workflows/deploy.yaml
    with:
      module: flok
      heroku-app: flok-staging
    secrets:
      HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
  auto-deploy-flok-admin-staging:
    needs: changes
    if: ${{ needs.changes.outputs.flok-admin== 'true' && github.ref == 'refs/heads/staging' }}
    uses: ./.github/workflows/deploy.yaml
    with:
      module: flok-admin
      heroku-app: flok-admin-staging
    secrets:
      HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
